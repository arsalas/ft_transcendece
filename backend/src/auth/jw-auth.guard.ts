// import { Injectable } from "@nestjs/common";

// @Injectable()
// export class JwtAuthGuard extends AuthGuard ('jwt') { // exporta el apartado que implementa la estrategia jw
//     // Queremos proteger la ruta tenemos que ir al apartado de Controller
// canActivate(context: ExecutionContext) : boolean | Promise<boolean> | Observer

// }
